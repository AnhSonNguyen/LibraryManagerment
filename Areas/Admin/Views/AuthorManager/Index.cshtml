@model IEnumerable<LibraryManagerment.Models.TbAuthor>

@{
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<div class="content-wrapper">
    <div class="content-header">
        <div class="container-fluid">
            <div class="row mb-2">
                <div class="col-sm-6">
                    <h1 class="m-0">Quản lý tác giả</h1>
                </div>
                <div class="col-sm-6 text-right">
                    <a asp-area="Admin" asp-controller="AuthorManager" asp-action="Create" class="btn btn-primary">
                        <i class="fas fa-plus"></i> Thêm tác giả mới
                    </a>
                </div>
            </div>
        </div>
    </div>

    <section class="content">
        <div class="container-fluid">
            <div class="card">
                <div class="card-header">
                    <h3 class="card-title">Danh sách tác giả</h3>
                </div>
                <div class="card-body p-0">
                    <div class="table-responsive">
                        <table class="table table-bordered table-hover mb-0">
                            <thead class="thead-light">
                                <tr>
                                    <th>ID</th>
                                    <th>Tên tác giả</th>
                                    <th>Mô tả</th>
                                    <th>Thao tác</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td>@item.AuthorId</td>
                                        <td>@item.AuthorName</td>
                                        <td>@item.Description</td>
                                        <td>
                                            <a asp-area="Admin" asp-controller="AuthorManager" asp-action="Edit" asp-route-id="@item.AuthorId" class="btn btn-sm btn-warning mr-1">
                                                <i class="fas fa-edit"></i> Sửa
                                            </a>
                                            <a asp-area="Admin" asp-controller="AuthorManager" asp-action="Delete" asp-route-id="@item.AuthorId" class="btn btn-sm btn-danger">
                                                <i class="fas fa-trash-alt"></i> Xóa
                                            </a>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
                <div class="card-footer text-muted">
                    Tổng số: @Model.Count() tác giả
                </div>
            </div>
        </div>
    </section>
</div>
